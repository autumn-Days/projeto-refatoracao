# Versões

01: A versão original do projeto refatorado. A única modificação foi a substituição de uma biblioteca (parser) que não existe mais nas versões mais atuais do python, ao invés dela foi usado a ast, que é equivalente

02: A versão original com o incremento da funcionalidade de mudar de modo.

*03:* A versão refatorada.

Para executar qualquer uma das versões, basta rodar "python3 main.py" no linux, já no windows o camando é "python main.py".
